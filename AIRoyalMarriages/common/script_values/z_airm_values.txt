z_airm_age_difference = {
	value = z_airm_age_difference_pre
	if = {
		limit = { z_airm_age_difference_pre < 0 }
		multiply = -1
	}
}

z_airm_age_difference_pre = {
	value = age
	scope:target = {
		subtract = age
	}
}